# Execution log

| date | prompt | gate | agent / model | result | commit | notes |
|---|---|---|---|---|---|---|
| 2026-09-14 | (package assembly, cloud sandbox) | E1, E2, E3, S1, CS | Claude Fable 5.1 (cloud session) | all PASS | see git log | Qiskit/CUDA-Q not installable in the sandbox (PyPI blocked): circuit translators written, numpy reference verified; laptop gates L2–L5 pending |
| 2026-09-14 | (independent audit, cloud session) | E1–S1, circuits | reviewer (Opus, independent rebuild of 2x2 in the 160 000-dim space) | physics confirmed; fixes applied: CUDA-Q kernel via real source file + endianness test, Table-3 device-row saturation now marked with the realised |B|, Kato–Temple denominator guard, CIPSI denominator magnitude, locality check in `localize`, static counts computed in E3, CI runs E1, README numbers sourced | see git log | audit findings recorded here for the record |
| 2026-09-14 | prompts/00_bootstrap_and_push.md (via RUNBOOK.md Phases 0 and 2) | package check, GitHub repo, first push | Claude Fable 5.1 (RUNBOOK session, inline) | PACKAGE OK; 14 tests passed; repo created and pushed; CI run 34905998241 green (all steps) | 54c131f | Python 3.12.14, numpy 2.5.2, scipy 1.18.0 (conda env `coding`); branch kept as `master`; https://github.com/digonto10602/su2qc-skqd |
| 2026-09-14 | prompts/01_laptop_L1_reproduce_cloud_gates.md | L1 (E1, E2, E3, S1 on the laptop) | Claude Fable 5.1 (RUNBOOK session, inline) | all four PASS; E1, E2, E3 criteria identical to the cloud copies; S1 differs in one criterion only (ridge Spearman B=0: 0.862 laptop vs 0.861 cloud, threshold > 0.7) | 54c131f | runtimes E1 427 s, E2 15 s, E3 36 s, S1 223 s (cloud 104 / 8 / 13 / 75 s); cloud JSONs kept in validation/cloud/ (gitignored) |
| 2026-09-14 | prompts/02_laptop_L2_qiskit_check.md | L2 | Claude Fable 5.1 (RUNBOOK session, inline) | PASS (26 s): max |Qiskit statevector − reference| = 3.725e-15 over 35 circuits; 10000/10000 noiseless shots decode; TVD 0.0103 < 0.272 | 6972fd5 | CPU only: qiskit 2.5.2, qiskit-aer 0.17.2; the qiskit-aer-gpu wheel (latest 0.15.1) is incompatible with qiskit 2.5.2 (ImportError convert_to_target) and was rolled back |
| 2026-09-14 | prompts/03_laptop_L3_cz_counts_S2.md | L3 | Claude Fable 5.1 (RUNBOOK session, inline) | FAIL as expected (baseline, 15 s): CZ per coarse step 35606 all-to-all, 55459 routed on heavy-hex distance 3 (budget ≤ 250) | 41b83c1 | level 3; per term: diag 8, hop0 16806, hop1 990, hop2 16790, hop3 990, plaq0 (structured) 30; not an escalation |
| 2026-09-14 | prompts/10_L5_fix_20260914.md | L5 | executor-opus | PASS (runtime 3.6 s) | (uncommitted: L4 run in flight, manual commit needed) | CUDA-Q 0.15.1 SampleResult has no .keys(); the two convention tests now use most_probable(). Conventions measured: result strings list qubit 0 first, BIG_ENDIAN=True (already the module default, circuits_cudaq.py unchanged). register_operation accepted the 256x256 hopping unitaries. Target qpp-cpu: the nvidia target aborts on the GTX 1060 Max-Q (compute capability 6.1 < 7.0), so GPU timing needs the RTX 3070 desktop or the cluster. TVD 0.017 < 0.272, 10000/10000 shots decoded, 0 rejections. |
| 2026-09-14 | prompts/09_L4_fix_20260914.md | L4 | executor-opus | FAIL (expected: yield criterion; both Weinstein criteria PASS) | (uncommitted; planner commits — HEAD 30684f2) | Added --pilot-shots/--min-shots (defaults 1000/500 = old behaviour) + stdout progress lines + pilot_shots/min_shots in R.data and the report header. Two reduced runs finished inside the 1800 s timeout: p2=3e-3 budget 25 min -> 1376 s (B=0: 2.163->3.312 s/shot, 11 shots x 20 circuits; B=1: 47 x 8), p2=1e-3 budget 12 min -> 1012 s (B=0: 8 x 20; B=1: 22 x 8). Mean CZ 35670 per coarse step => f=(1-p2)^35670 is numerically 0, so the yield criterion cannot pass with the generic-synthesis circuits (S2 depth problem, S2-b). Noisy Aer sampling costs ~2-3.3 s/shot on this laptop (CPU only, no qiskit-aer-gpu): a full 1000-shot-pilot + 500-shot-floor run would need ~7-14 h here; RTX 3070 desktop / Slurm GPU or shallower S2-b circuits needed. |
| 2026-09-15 | prompts/06_S2b_plaquette_interior_and_hopping_decomposition.md (stage 1: 2x2 hopping) | S2 (stage 1) | executor-opus | structured 2x2 hopping gates, exact basic-gate circuits; coarse step 264 CZ all-to-all, 655 routed (heavy-hex d=3) at this commit, improved to 256/618 by the stage-2 real gauge; L2 PASS (9 s) after the switch | 5d60461 | New engine circuits_ir.structured_term_gates: two-level (Givens) rotations per block, merged per flip pattern into Gray-code uniformly controlled rotations, control set minimised by an invariance-subspace search over non-codeword strings. New IR gates ry, rx, gphase, mcu; CircuitFactory(structured_hopping=False) keeps the dense L3 path (L3 and CS pinned to it, L3 re-run reproduces 35606/55459). Per-term CZ all-to-all after stage 2: diag 8, hop0 60, hop1 48, hop2 66, hop3 44, plaq0 30 (at this commit: 56/52/64/54). Max deviation vs the dense local unitary 1.3e-15 (20 random physical states, theta = dt, 2dt, 4dt). |
| 2026-09-15 | prompts/06 (stage 2: 2x3 hopping and interior-corner plaquettes) | S2 (stage 2) | executor-opus | all seven 2x3 links and both 14-qubit plaquettes compiled exactly to basic gates; coarse step 2164 CZ all-to-all, 5477 routed (heavy-hex d=5) | e6275bb | Blocks are bipartite with zero diagonal, so exp(-i theta h) = (U+V) [pair rotations] (U+V)^dag from the SVD of the off-diagonal part -- the three-configuration Givens chain generalised to the four- and five-configuration chains of 2x3; a diagonal gauge makes every block generator purely imaginary so all two-level rotations are real (one uniformly controlled Ry per step). The dense 14-qubit plaquette unitary (4 GiB) is never built: verification uses localize's block h on 2^14 and, for plaq1, the full 2^20 statevector against expm_multiply. Per-term CZ all-to-all: diag 20, hop0 194, hop1 48, hop2 182, hop3 184, hop4 358, hop5 156, hop6 44, plaq0 214, plaq1 764. 20 pytest tests pass in 86 s. |
| 2026-09-15 | prompts/06 (stage 3: gate S2) | S2 | executor-opus | FAIL on the CZ budget only (437 s): 2x2 routed 618 (<= 250), 2x3 routed 5477 (<= 500); exactness 1.9e-14 (< 1e-10), compiled leakage 4.5e-14 (< 1e-9), pytest green | e6275bb | New scripts/gate_S2.py (validation/S2.json, reports/S2_structured_circuits.md, enriched validation/BLOCKED.md, all generated from the JSON). Not pushed (FAIL). Improvement over the L3 baseline at 2x2: 55459 -> 618 routed (factor 90), 35606 -> 256 all-to-all; at 2x3 no baseline existed. Dominant terms (routed): 2x2 hop2 149, 2x3 plaq1 1698. Cost drivers measured per term in the report: number of multiplexed rotations (5 at 2x2 per link, 9 at 2x3, 17 for plaq1) x 2^(controls) (2-4 at 2x2, 4-7 at 2x3), then a factor 2.4-2.5 for heavy-hex routing (degree 3 vs star-shaped multiplexed rotations). Planner decision needed between the three escalation options of prompts/06; the budget was NOT relaxed. Runtime 7 min, laptop CPU only, well inside the 30-minute rule. |
| 2026-09-15 | prompts/11_S2_planner_decision_20260915.md (planner) | S2 escalation | planner-fable (max) | diagnostics recorded: no coupling map or term ablation reaches the budget (data/S2_escalation_experiments.json); next executor step = fixed-angle floor measurement; owner decision (a)/(b)/(c) pending | see git log | reports/S2_escalation_analysis.md |
| 2026-09-15 | prompts/11_S2_planner_decision_20260915.md (steps 1-5) | S2 escalation (measurement, no gate PASS claimed) | executor-opus | fixed-angle generator measured: leak-free (<= 2.0e-14), S1 recall KEPT (B=0 1.000, B=1 0.937 at f = 0.1), but the cost floor is 3736 routed CZ at 2x3 (budget 500, stop threshold 1500 of prompts/11) -> STOPPED after step 3 as the prompt requires; steps 4-5 finished: L5 PASS (4 s), L2 PASS (9 s), 24 pytest tests green | 85f561e, 0420211, a4da83d | `angle_mode="fixed"` in circuits_ir (same codeword pairs, same validity controls, one angle per qubit-flip pattern, theta_eff = theta x mean of the distinct |elements|, signs kept; the flattened generator splits into disjoint commuting pairs, so one multiplexed rotation per pattern is exact for it). CZ per coarse step all-to-all / heavy-hex / square grid: 2x2 240 / 671 / 474 (exact S2: 256 / 618), 2x3 1626 / 3736 / 2803 (exact S2: 2164 / 5477). Rotations per term drop (2x3 hopping 9 -> 4, plaq1 17 -> 4) but the controls RISE (2x3 hopping 3-6 -> 6, plaq1 4-7 -> 5-7): the angle-selecting controls were never the cost driver, the pair-selection (validity) controls are, so this lever is spent. Recall re-emulation at 2x3 (gate-S1 production budget, 2e5 shots/sector, all references, k = 1..4, coarse-step states from run_ir on the 2^20 statevector, seed 20260914, scripts/s2_fixed_recall.py): fixed B=0 1.000 / |B| 304, fixed B=1 0.937 / |B| 204 at f = 0.1; the exact-mode control rows reproduce gate S1 exactly (1.000 / 347 and 1.000 / 246), which validates the route. Runtimes 1190 / 467 / 1304 / 519 s per sector-mode (30-minute rule OK; run_ir gained in-place permutation and diagonal gate paths to get there). CUDA-Q: ry, rx, gphase (r1-x-r1-x) and mcu (registered op with .ctrl) added; L5 has a new IR-gate-coverage criterion. validation/S2.json and reports/S2_structured_circuits.md untouched; new validation/S2_fixed.json + reports/S2_fixed_angle_circuits.md + data/S2_fixed_recall*.json. Owner decision (a)/(b)/(c) of prompts/11 still required: option (a)'s premise (<= ~1000 routed) is NOT met. |
| 2026-09-16 | prompts/12_combined_plan_device_budgets_S3_prep.md | S2D (new), L4_fez | executor-opus | **S2D FAIL** (2 of 8 criteria: 2x3 mean f 0.0534 < 0.1 and 2x3 shot budget 4.61e6 > 2e5 per sector) — escalation clauses of prompts/12 triggered, reported and STOPPED, no threshold relaxed; **L4_fez PASS** (4/4); 25 pytest tests green | 726f2a5, 7c8150f, fe97e62, (this commit) | New `skqd.skqd.shot_rule(p, y, k=3, conf=0.95)` = ceil(lambda*/(p y)) with `poisson_lambda_star` (brentq on scipy.stats.poisson.sf): lambda*(3, 0.95) = 6.295794, reproduces the manual's 6.3 and its eq.-(5) example (f = 0.2, p = 1e-3 -> 38389 shots/circuit); unit test with a binomial Monte Carlo. New `scripts/gate_S2D.py` (validation/S2D.json, reports/S2D_device_budgets.md), circuit family unchanged (exact structured circuits, S2.json untouched), 28 circuits at 2x2 and 44 at 2x3 = references x k = 1..4. 2x2 on Heron snapshots (transpile onto the backend, level 3, seed 7; f = prod(1 - per-edge CZ error) x prod(1 - readout error) over the executed gates/qubits): FakeFez mean f 0.1248 / worst 0.1166 / 663 CZ / patch [117,122,123,124,125,136,141,142,143,144,145,146], patch edge error 3.05e-3 vs snapshot median 3.90e-3; FakeTorino 0.0735 / 0.0569 / 636 CZ (readout 3.51e-2 kills it) -> criterion f >= 0.1 PASSES on the best snapshot. 2x3 all-to-all RZZ at the DECLARED inputs eps2 1e-3, eps1 1e-4, eps_ro 2e-3 (recorded under `assumed_inputs`): 2158 RZZ + 7310 1q gates, f = 0.1154 (2q) x 0.4797 (1q) x 0.9608 (ro) = 0.0534 -> FAIL; eps2 needed for f = 0.1 at these counts: 7.1e-4; with virtual rz f = 0.0817, still < 0.1. Shot budget (p = 1e-3, k = 3, conf = 0.95, y = 0.82 f): 2x3 B=0 143921 x 32 = 4.61e6, B=1 143508 x 12 = 1.72e6 per sector -> FAIL vs 2e5; note that at the manual's own design point f = 0.2 eq. (5) already asks 1.69e6 per sector, so Step 9.2's quota and eq. (5) at p = 1e-3 are inconsistent in the manual itself. Reproduction checks PASS: 618 routed on ideal heavy-hex d=3 and 2164 all-to-all CZ, both = validation/S2.json. 2x3 noisy Aer sampling in the RZZ basis: 3.20 s/shot on this laptop CPU (10 shots timed) -> 4.09e3 CPU-hours for one B=0 sector at the budget above = a batched Aer GPU job (RTX 3070 / cluster), not a laptop job. L4 extended with --backend/--out (defaults unchanged): L4_fez PASS, measured yield 0.1438 / 0.1381 vs predicted 0.82 f = 0.1034 / 0.0995, ratio 1.39 in BOTH sectors (the manual's yield model is conservative; the 0.15 % uniform-garbage term cannot explain it), |B| = 38 / 20 (sectors saturate), recall 1.000, exact E0 inside both Weinstein intervals, 530 s. `proposal/amendment_01_devices_and_budgets.md` is a DRAFT generated by `scripts/make_amendment.py` from the two JSONs (91 cited values, provenance table, `--check-only` re-verifies); it asks the owner to sign 2x2-on-Heron and the f >= 0.1 criterion and leaves the 2x3 device and the shot quota OPEN. Deviation from the prompt: the fez report is `reports/L4_fez_aer_noise.md`, not `reports/L4_aer_noise.md`, so that each report keeps matching its own JSON. Runtimes 213 s (S2D) and 530 s (L4_fez), both far inside the 30-minute rule. |
| 2026-09-16 | prompts/12 follow-up (planner) | S2D check | planner-fable (max) | S1 criterion re-evaluated at the S2D-predicted 2x3 f: min recall 0.988 (B=0) / 0.958 (B=1) at f=0.0534, 2e5 shots per sector; per-sector shot rule N=1.24e5/1.25e5 <= 2e5 | see git log | scripts/s2d_recall_at_predicted_f.py, data/S2D_recall_at_f.json, amendment section 6 |
| 2026-09-16 | prompts/13_H0_preparation_heron_and_S3_desktop_job.md | H0P (new), H0_dryrun (new), S3_smoke (new) | executor-opus | **H0P FAIL** (3 of 16 criteria, all three the same physics: the simulated yield at r = 2 (B=0) and r = 3 (both sectors) is 3.0x / 8.3x / 5.6x the manual's 0.82 f, outside the factor-3 window; no threshold relaxed) — **H0_dryrun PASS** (9/9) and **S3_smoke** ran end to end (FAIL on the S3 recall criterion by construction: 2 shots per circuit); 31 pytest tests green | (this commit) | **1. Frozen H0 circuit set** `scripts/h0_build_circuits.py` -> `data/hardware/H0_prep/` (1.9 MB): 84 coarse-step circuits (B=0 60, B=1 24 = references x k = 1..4 x r = 1,2,3 repetitions of the step, U^r after one state preparation) plus 42 readout-calibration circuits (all-0, all-1, 12 single-qubit flips on each of the 3 readout patches the transpiler chose), transpiled onto FakeFez at level 3 seed 7, stored as gzipped QPY (`gzip.open` + `qiskit.qpy.dump`; 139-400 KB each raw, 7-17 KB compressed) with one manifest JSON per circuit (sector, reference, k, r, CZ, depth, physical qubits, logical->physical map, clbit->physical measurement map, codeword bit order, the 4 flux-bit link-consistency checks, and the S2D f of that snapshot). CZ per repetition 663 / 1305 / 1920 (prompt expectation 663 / 1326 / 1989), f 0.1248 / 0.0159 / 0.0025, depth 1340 / 2676 / 4008. Leak-freeness is verified on the TRANSPILED circuit: new `skqd.hardware.logical_statevector` compacts it onto the physical qubits it touches (level-3 routing uses 17 of them for 12 logical qubits), keeps the ancilla-all-zero slice and permutes back with `tq.layout.final_index_layout()`; max leakage 1.49e-14 over 84 circuits (tolerance 1e-9), and every circuit's measurement map equals its final layout, which is what makes `qiskit_key_to_bits` valid. **2. Gate H0P** `scripts/gate_H0P.py` (validation/H0P.json, reports/H0P_heron_preparation.md, reports/H0_prereg_draft.md): `AerSimulator.from_backend(FakeFez(), seed_simulator=11)`, 267/130/92 shots per r=1/2/3 circuit (13692 shots, 1031 s sampling, 1299 s total, 14-min budget; a two-point pilot separates the ~6 s per-call setup from the per-circuit-shot cost 0.033/0.076/0.102 s and the shots are allocated equal-time per repetition class). Yield table (sector, r, CZ, f, 0.82 f, simulated, ratio): B=0 663 0.1261 0.1034 0.1493 **1.44**, B=0 1308 0.0154 0.0127 0.0381 **3.01**, B=0 1910 0.0026 0.0021 0.0174 **8.28**, B=1 663 0.1214 0.0995 0.1353 **1.36**, B=1 1296 0.0170 0.0139 0.0288 **2.07**, B=1 1944 0.0024 0.0020 0.0109 **5.57**. PASS: leakage, both Ritz energies exact (|E_R - E_0| = 0.0, supports 38/38 and 20/20, r_H 6.1e-15 / 1.5e-15), random-string acceptance 0.928 % / 0.488 % (exhaustive over all 4096 strings) < 1 %, readout confusion diagonal min 0.9768 mean 0.9912 over the 3 patches, the three artefacts, pytest. **Diagnosis of the FAIL (added as a diagnostic column, not a criterion): the decoder's false-acceptance floor.** A non-clean shot is still accepted whenever its string is a codeword of the target sector, which happens with probability a = 0.00928 (B=0) / 0.00488 (B=1), so the yield cannot fall below (1-f) a and 0.82 f only describes it while f >> a/0.82 = 0.011. The floor-corrected model 0.82 f + (1-f) a reproduces ALL SIX points within 1.30-1.75x (vs 1.36-8.28x for 0.82 f). The r = 1 points, the ones prompts/07 judges f on, pass at 1.44 / 1.36, consistent with L4_fez's 1.39. Planner decision needed: either the yield model of manual Step 4.4 gains the (1-f) a term, or the H0P criterion is restricted to circuits with 0.82 f >> a (i.e. r = 1 at this device). Re-running H0P with the shots pinned reproduced every criterion value exactly (deterministic). **3. Submission path** `scripts/h0_submit.py`: `qiskit_ibm_runtime.SamplerV2` with dynamical decoupling (XY4), gate and measurement Pauli twirling ('active-accum') and no error mitigation; pubs are grouped into one job per shot count (0.48 deprecates mixed shots in a job) and chunked at 50 pubs. `--dry-run` uses local testing mode on `AerSimulator.from_backend(FakeFez())` (the runtime warns that DD/twirling have no effect locally -- recorded in `session.json` as `sampler_options_effective: false`); counts are written one JSON per circuit with the manifest, the raw qiskit-order counts and the key convention, and the script refuses to overwrite an existing counts file. The committed dry run used seed 21 (independent of H0P's seed 11), 267/130/92 + 4000 calibration shots, 1044 s. `scripts/gate_H0.py` reads any counts directory and applies the prompts/07 criteria -> **validation/H0_dryrun.json PASS (9/9)**: 58 of 58 distinct accepted strings re-encode to themselves, random-string acceptance 0.928/0.488 %, measured f 0.1804 vs predicted 0.1820 (B=0, deviation 0.9 %) and 0.1621 vs 0.1650 (B=1, 1.7 %) against the preregistered *simulated* prediction of H0P (criterion 30 %), both Ritz energies exact with saturated supports (38/38, 20/20), confusion diagonal min 0.9764, and the measured per-qubit readout error within 1.28x of the frozen snapshot value (the calibration-drift item that is expected to move on a real device). **4. S3 job** `scripts/s3_device_model.py` + `slurm/s3_2x3.sbatch`: one command per lattice/sector/repetition, `--backend FakeFez` (NoiseModel.from_backend) for 2x2 or the declared all-to-all RZZ model for 2x3, Aer `device='GPU', batched_shots_gpu=True` iff `AerSimulator().available_devices()` contains 'GPU' (this laptop: CPU only). Smoke test `--lattice 3 --sector 1 --shots-per-sector 24 --tag smoke`: 12 circuits x 2 shots, 2158 RZZ each, 295 s, yield 0.417 (10/24), |B| = 7 of a 426-dim sector, recall 0.074 (criterion 0.9 -> FAIL by construction), exact E0 = -3.8261 inside the Weinstein interval [-4.6153, -3.1583]. Cost: 10.42 s per shot at 2 shots per call (dominated by the fixed per-call setup) -> 579 h per 2e5-shot sector on this CPU; at the S2D rate of 3.20 s/shot (10 shots per call) the same sector is 178 h, i.e. the four Step-9.2 jobs are a desktop-GPU / cluster job either way. **5. New `src/skqd/hardware.py`**: `confusion_matrix(counts_by_prep)` (tensored per-qubit readout model, columns normalised), `apply_inverse(counts, C)` (exact tensor inverse, quasi-counts), `transpiled_layout`, `logical_statevector`, `codeword_bit_order`, `link_consistency_checks`; 6 new unit tests (31 total). No physics convention touched; `validation/S2.json`, `S2D.json`, `L4_fez.json` untouched. Every run inside the 30-minute rule (longest 1301 s). |
| 2026-09-16 | prompts/14_H0P_fix_yield_model_20260916.md | H0P (re-run), L4_fez, H0_dryrun | executor-opus | **H0P PASS** (16/16, 1182 s) after the yield model gained the term the manual already names; **L4_fez PASS** (4/4, 450 s), **H0_dryrun PASS** (9/9); 32 pytest tests green | 41877c1, a82a911, 3722018, (this commit) | **The fix.** Manual Step 4.4 (line 130): "the accepted-shot yield is ~ 0.82 f plus the 0.15 % of garbage that decodes as valid"; the gate scripts implemented only the first term. New `skqd.skqd.yield_model(f, a, readout_factor=0.82) = 0.82 f + (1-f) a` and its inverse `clean_fraction_from_yield(y, a) = (y - a)/(0.82 - a)`, with a = the decoder's random-string acceptance of the TARGET sector, computed exhaustively (all 4096 strings through `Codec.decode`): a = 0.00928 (B=0) / 0.00488 (B=1) at 2x2, and 0.00146 at 2x3 from validation/E2.json (any sector, an upper bound). Unit test covers the a = 0 limit, both f limits, monotonicity, round trips and a non-0.82 readout factor. **Nothing was relaxed**: the factor 0.82, the [1/3, 3] window, the 30 % H0 criterion, the shots and the seeds are untouched, and the shot rule keeps using the CLEAN yield 0.82 f in gate_S2D and in the prereg draft (a garbage acceptance adds no support) - S2D's N_circuit/N_sector are unchanged, it only gains an informational full-model column. **H0P re-run** (`--shots-by-rep 1:267 2:130 3:92 --budget-minutes 14 --pilot-shots 60`, seed 11): the six simulated yields are IDENTICAL to the FAIL run of prompts/13 (0.14925094 / 0.03807692 / 0.01739130 / 0.13529963 / 0.02884615 / 0.01086957; frozen_set and shot_plan blocks byte-identical), only the model column and the criteria changed. Ratios simulated / model, per sector r = 1, 2, 3: B=0 1.44 -> **1.34**, 3.01 -> **1.75**, 8.28 -> **1.53**; B=1 1.36 -> **1.30**, 2.07 -> **1.54**, 5.57 -> **1.59** - all six inside [1/3, 3], the residual 1.30-1.75 being the known conservatism of the 0.82 readout factor (L4_fez measures 1.32 / 1.31 on the same snapshot). **L4_fez** re-run (`--backend FakeFez --budget-minutes 20 --pilot-shots 50 --min-shots 1`): 258 / 722 shots per circuit (pilot 0.116 / 0.104 s per shot), measured yield 0.1467 / 0.1356 vs full model 0.1115 / 0.1038 (1.32 / 1.31) and vs 0.82 f alone 0.1034 / 0.0995 (1.42 / 1.36); |B| 38 / 20, recall 1.000, both exact E0 inside the Weinstein intervals. **H0_dryrun** re-analysed from the untouched counts (`--out H0_dryrun`, 126 counts files, never re-sampled): the measured f now comes from `clean_fraction_from_yield`, B=0 0.1710 vs predicted 0.1727 (deviation 0.9 %) and B=1 0.1571 vs 0.1600 (1.8 %), both far inside the 30 % criterion (with y/0.82 they were 0.1804 vs 0.1820 and 0.1621 vs 0.1650). `reports/H0_prereg_draft.md` regenerated: it states the full model, a, the inverse used for the measured f, and its shot plan now uses the clean yield 0.82 f. Runtimes 1182 s (H0P) and 450 s (L4_fez), inside the 30-minute rule; the escalation clause of prompts/14 was NOT reached. Note: `scripts/run_gate.py L4 ... --out L4_fez` reads validation/L4.json for its status line, so it prints the stale L4 FAIL and rewrites the (gitignored) validation/BLOCKED.md; validation/L4_fez.json is the gate result and says PASS. |
| 2026-09-21 | prompts/15_H0_session_ibm_fez.md (planner) | H0 (session plan; part A engineering, part B the ibm_fez session) | planner-fable (max) | prompt written; nothing run, nothing submitted, no threshold changed | (uncommitted; the planner writes prompts, the caller commits) | Inputs verified this session: IBM account on the open plan (channel ibm_quantum_platform, instance open-instance), ibm_fez / ibm_marrakesh / ibm_kingston reachable (Heron r2, 156 qubits, operational), the frozen set of data/hardware/H0_prep runs "as frozen" on all three (30 qubits, 54 edges, basis cz/rz/sx/x/measure); owner chose ibm_fez. Execution-time estimate from the live target (ASAP schedule, shot plan 1:267 2:130 3:92, 4000 calibration shots): 181692 shots, 46.7 s = 0.78 min of the 10 min/month, 42.3 s of it readout calibration. Planner decisions in the prompt: D1 circuits stay frozen, only the prediction is recomputed on the day from the live target (gate_H0P.py --backend ibm_fez -> validation/H0P_ibm_fez.json + reports/H0_prereg_ibm_fez.md; the builder's live path is a contingency re-freeze to a NEW directory); D2 calibration stays at 4000 shots (the factor-3 readout-drift item needs ~19.5 expected error events on the best qubit, e = 0.0024; at 1000 shots the chance of a spurious failure among the 36 qubit entries is 0.81); D3 coarse-step shots stay like-for-like with H0P (r = 1 two-sample sigma of the f ratio 4.9 % / 8.0 % vs the 30 % criterion); D4 submit-then-retrieve split REQUIRED (job ids to session.json before the next sampler.run, --retrieve --wait <= 1500 s); D5 canary job first (B0_ref06_k1_rep1 + cal_patch1_all0/all1 at 267 shots, 0.21 s of execution; go rule >= 10 accepted of 267, predicted 39.9, garbage-only 2.5); D6 budget caps: estimate <= 120 s, reported usage <= 180 s, no resubmission without the planner; D7 the drift item references the day's calibration (factor 3 unchanged; the 2025 FakeFez snapshot comparison becomes informational); D8 sampler options frozen as in the dry run. Two owner stop points (before the canary, before the main submission). H0 criteria unchanged (round trip, random acceptance < 1 %, r = 1 f within 30 % via clean_fraction_from_yield, E0 to 1e-6, confusion diagonal >= 0.9). Escalation keeps prompts/07: measured f < 0.7 x predicted at 663 CZ -> the 2x3 budget is not spent, Plan B (k = 1, 2). |
| 2026-09-21 | prompts/15_H0_session_ibm_fez.md (**part A only**; part B not started, nothing submitted, no QPU time spent) | H0P_repro (new), H0_dryrun (re-analysed) | executor-opus | **part A criteria all met**: h0_compare_prep 126/126 identical (exit 0); **validation/H0P_repro.json PASS** (15 criteria, `--no-tests` drops H0P's 16th, the pytest one; the other 15 identical in name AND value to validation/H0P.json) and the one-liner prints "H0P_repro identical"; **validation/H0_dryrun.json PASS 9/9** with every `criteria[*].value` identical to the committed file and the whole `data` block byte-identical (only two criterion NAMES gained the support-size diagnostic, plus runtime_s and the environment block); dry-run submit/retrieve split exercised 4 ways; **44 pytest tests green** (12 new) | (this commit) | **A1 live-backend resolution.** New `scripts/h0_backends.py`: `resolve_backend(name)` returns `FakeFez()`/`FakeTorino()` for the snapshots and `ibm_account.open_service().backend(name)` for anything else (a name the account cannot reach is a clean SystemExit listing what it can); `last_update_date`, `calibration_stamp` (ISO -> `YYYYMMDDTHHMMZ` in UTC) and `calibration_record(backend, qubits, edges)` = the day's calibration of exactly the qubits and edges the frozen set uses (measure/sx/x error + duration, rz duration, T1, T2, cz error + duration per target key, dt, default rep delay + range, max_circuits, basis, status, and `missing_errors`). `h0_build_circuits.py --backend` is now free-form (default FakeFez, unchanged): for a live backend `--out` is mandatory and `data/hardware/H0_prep` is refused (D1), and the manifests record `backend_calibration_last_update`. `gate_H0P.py` gains `--backend`, `--calibration-dir`, `--no-prereg`: on a live backend it writes `data/hardware/H0_<backend>/calibration_<stamp>.json`, **stops** if `missing_errors` is non-empty (no defaulting), recomputes f per circuit with `gate_S2D.analyse_on_backend(frozen circuit, live target)`, asserts the loaded QPY still carries each manifest's CZ count, uses the live f as `f_calibration_mean` in the yield table and the criteria, keeps the manifest value as the extra column "f (frozen snapshot)" and in `data.f_recomputed_on_the_day`, and writes `reports/H0_prereg_<backend>.md`. The criteria list is unchanged (16 with pytest), so B3 is still judged on H0P's own 16. `analyse_on_backend` was VERIFIED on the live ibm_fez target (read-only metadata, no QPU time): all **54 cz and 30 measure `.error` values non-None** (`missing_errors == []`), canary circuit `B0_ref06_k1_rep1` cz 663 = manifest, f 0.178843 live vs 0.126131 in the manifest, mean edge error 2.348e-3, mean readout 9.583e-3, calibration `last_update_date` 2026-09-21T14:53:59-06:00 (stamp 20260921T2053Z), operational, 0 pending jobs. Note for `calibration_record`: the frozen set's 54 edges are DIRECTED pairs and the live/snapshot target keys both directions separately, so all 54 are recorded and checked individually (`n_directed_pairs_frozen_set` 54, `n_edges_frozen_set` 54); an earlier version that deduplicated to undirected pairs reported 29 and was fixed. New `scripts/h0_compare_prep.py` compares two prep directories by `QuantumCircuit.__eq__`, `count_ops()`, `layout.final_index_layout()` and the manifest fields cz, depth, physical_qubits, logical_to_physical, measurement_map_clbit_to_physical, f_calibration_snapshot (1e-12) - not the .qpy.gz sha256, which carries a gzip timestamp. Rebuild of the frozen set into /tmp/H0_prep_repro (126 s) is **126/126 identical** and `git status data/hardware/H0_prep` is clean. **A2 gate_H0.py**: `--calibration <json>` makes the day's per-qubit measure errors the reference of the factor-3 drift item (`readout_vs_calibration`, `readout_reference` in the JSON, two extra report columns); the frozen-snapshot comparison stays in the table and becomes `readout_vs_snapshot_informational` (no criterion) when the manifest backend differs from the counts' backend, and stays a criterion when they are the same - the dry run keeps its 9th criterion because counts files written before this change carry no `backend_manifest` (absent = same device) and the new ones record it (`FakeFez` vs `ibm_fez`). New `error_ratios()` guards a measured error of exactly 0 (ratio None, the qubit listed under `qubits_without_a_readout_error_event`, no ZeroDivisionError; None if no qubit has an error event). The E0 criterion line now reads "support N decoded + references = M of the K-dimensional sector: saturated / NOT saturated", which is what distinguishes a non-saturated support from a bit-order error. Both branches were exercised end to end: the canary dry-run counts give "11 decoded ... of the 38-dimensional sector: NOT saturated" and a synthetic copy tagged `backend: ibm_fez` switches the snapshot item to informational and evaluates the drift item against the live calibration file. **A3 submit/retrieve split in h0_submit.py**: phases `--submit` (implied by `--backend`/`--dry-run`), `--retrieve [--wait S <= 1500]`, `--status`, plus `--resume` and `--only`. `session.json` now has one entry PER JOB (group_shots, chunk_index, n_pubs, circuit_ids, job_id, submitted, status, usage_s, usage_raw, metrics, retrieved, counts_written, counts_files_written/skipped) and the per-circuit list moved to `circuits`; every job id is written atomically (tmp + rename) BEFORE the next `sampler.run`, a failed submission is recorded and exits non-zero, and `total_usage_s` is the sum over jobs. Live preflight: backend name, `status().operational`, `check_backend(frozen_requirements(prep))`, `max_circuits` vs the largest chunk, the live `properties().last_update_date` equal to `validation/H0P_<backend>.json`'s (else "re-run gate_H0P.py --backend ibm_fez"), the `h0_qpu_time.py` estimate <= `--max-qpu-seconds` (default 120), and no existing counts file - all before a single pub is built. Counts files gained `backend_manifest`, `retrieved` and `usage_s`. **Laptop tests**: (i) `--dry-run --shots-by-rep 1:4 2:4 3:4 --cal-shots 4 --out /tmp/h0_split_test` -> 126 counts files, every job with a non-null id and `counts_written: true`, but **3 jobs, not the 4 the prompt's criterion names**: with those flags all four groups collapse to shots = 4, so the single group of 126 pubs is chunked 50/50/26 by `--max-pubs-per-job`. Grouping stays "one job per distinct shot count" because that is what makes the canary ONE job of 3 pubs (D5) and the main session exactly 4 jobs; `--dry-run --shots-by-rep 1:4 2:5 3:6 --cal-shots 7 --out /tmp/h0_split_test4` reproduces the production structure - **4 jobs of 28/28/28/42 pubs**, all DONE, all `counts_written: true`, 126 counts files. (ii) the repeated command exits 1 BEFORE any sampling ("counts files are raw data"). (iii) `--retrieve` on a dry-run session exits 1 with "local jobs cannot be retrieved by id". (iv) the canary dry run writes 3 counts files in 1 job and `gate_H0.py` runs to completion on them without a traceback (status FAIL as predicted: support not saturated, and 267 calibration shots make the readout item meaningless); its validation/report artefacts were deleted. **A4 `scripts/h0_qpu_time.py`**: ASAP critical path over `target.durations()` per circuit, then circuits x shots x (duration + rep_delay), with the per-group table, total_shots, total_execution_s, dt, rep delay, basis durations and the target's last_update_date. On the **live ibm_fez target it reproduces the planner's 2026-09-21 numbers**: total **46.7 s** over 181692 shots (planner 46.7 s), r = 1 45.5 us / r = 2 88.5 us / r = 3 128.3 us per circuit against 46.0 / 89.2 / 133.9 us (1.1 %, 0.8 %, 4.2 % - all inside the 10 % of A4; the device was recalibrated at 14:53 today), readout calibration 1.7 us x 4000 shots x 42 circuits = 42.3 s, rep delay 250 us, dt 4e-9. The offline `--backend FakeFez` check gives 46.8 s with the snapshot's longer durations (52.7 / 103.0 / 149.4 us). **A5**: 44 pytest tests pass (2:14); 12 new in `tests/test_h0_scripts.py` cover the `resolve_backend` name routing (fake names need no account, a live name goes through a stubbed `open_service`, an unreachable name is a SystemExit), `calibration_stamp` UTC conversion, `calibration_record` on the snapshot (no missing errors, every field populated) and with a `.error` forced to None (it lands in `missing_errors` instead of defaulting), the ASAP critical path (parallel vs serial gates, rz/barrier cost nothing) and the three `error_ratios` guards. `scripts/check_package.py` PACKAGE OK; `update_status.py` gained the `H0P_ibm_fez` row (the canary is not a gate row) and the report/next-action lines; `graphify update .` run. **Nothing in `data/hardware/H0_prep` was touched** (git status clean), no threshold, convention or frozen constant changed (F_TOLERANCE 0.30, RANDOM_ACCEPT_MAX 0.01, E0_TOL 1e-6, DIAG_MIN 0.9, RO_FACTOR 3, READOUT_FACTOR 0.82 untouched). One generated-prose change: the prereg template's "re-run h0_build_circuits.py --backend <device>" sentence now states D1 (circuits frozen, prediction recomputed); the committed `reports/H0_prereg_draft.md` still shows the old sentence because `--no-prereg` was used for the repro run, and the new wording will appear in `reports/H0_prereg_ibm_fez.md` when B3 runs. Longest single command 1146 s (H0P_repro), well inside the 30-minute rule. **Part B was NOT started**: no submission, no canary, no QPU seconds; the only live access was read-only `backend.target` / `properties()` / `status()`. |
| 2026-09-21 | prompts/15_H0_session_ibm_fez.md (**part B, steps B1-B3 only**; held at STOP A, nothing submitted, 0 QPU seconds spent) | H0P_ibm_fez (the day's prediction) | executor-opus | **validation/H0P_ibm_fez.json FAIL** (15 of 16 criteria PASS). B1 PASS: ibm_fez operational, "OK: the frozen set runs on ibm_fez as frozen". B2 PASS: `total_execution_s` 46.7 s <= 120 s (D6), unchanged from the planner's 2026-09-21 value. B3: prediction recomputed on the live calibration `last_update_date` 2026-09-21T14:53:59-06:00 (stamp 20260921T2053Z, `missing_errors` []), 23 pending jobs at prediction time, runtime ~19 min. The B3 stop rule did **not** fire: r = 1 simulated yields 0.1657 (B=0) / 0.1554 (B=1) vs 0.1493 / 0.1353 on the FakeFez snapshot, live f 0.1788 / 0.1782 vs 0.1261 / 0.1214; confusion min diagonal 0.9624. The one failing criterion is criterion 3 of the prereg in the B=1 sector: the device-model simulation decodes `support_size_decoded` 19 of `sector_dimension` 20, so \|E_R - E_0\| = 5.749e-06 > 1e-06 (rH 0.00595, Weinstein bracket still contains E0, `captured_weight` 0.9999990674 -- the missing state carries 9.3e-07 of the ground-state weight). The same seed and shot plan saturated 20/20 on the FakeFez calibration. | 51ac0e6 | Held at B4 STOP A and handed back: the prompt's part-B pass criterion for B3 is "status PASS", so the canary was not submitted and no owner go was acted on (the two go/no-go points are the owner's; an agent message is not a go). Planner call needed: with the frozen shot plan (D3) the 20th B=1 state is populated only by chance through noise, so gate H0's criterion 3 is at risk on the device for the same reason -- escalation branch (a), `support_size_decoded < sector_dimension`, would apply. `data/hardware/H0_prep` verified git-clean throughout; laptop commands all < 30 min (B3 ~19 min). |
| 2026-09-21 | prompts/16_H0P_ibm_fez_fix_20260921.md (planner; escalation of the STOP-A hold of prompts/15 part B) | H0P_ibm_fez (FAIL 15/16 at 51ac0e6), H0 (open) | planner-fable (max) | diagnosis written (reports/H0P_ibm_fez_escalation_20260921.md) and fix prompt written; nothing run on the QPU, 0 QPU seconds; no criterion constant or tolerance changed; frozen item 3 of prompts/15 (the r = 1 shot counts of D3) replaced by the rule D3' and two preparation criteria added to gate H0P, both named in the prompt | (uncommitted; the planner writes prompts, the caller commits) | Verified from the JSONs and by planner computation with repository code (cross-checked against the frozen QPY to 5e-15): the support is sampled from the coarse-step states, in which the missing B=1 state (one of 40/53/61/65, ground-state weight 9.33e-7; probably 53) has ideal probability 3.1e-3 to 4.6e-2, so its expected clean count under the frozen plan is 0.33, not the executor's 3e-4 (which used the ground-state weight). Under the plan 267/130/92 the sectors saturate from clean shots with probability 0.004 (B=0) / 0.076 (B=1) on the live calibration; garbage acceptances ((1-f) a: 2.15 / 0.86 expected hits per state) lift this to 0.60 / 0.42, so criterion 3 was a coin flip in BOTH sectors in every H0P run so far (the 2026-09-16 PASS had P = 0.18) and would be one on the QPU. Options: (i) waiting for a saturating calibration = cherry-picking the seed, rejected; (ii) Weinstein-based relaxation, rejected as post hoc; (iv) new references, rejected (frozen set, does not fix B=0); (iii) adopted and generalised: the manual's shot rule (eq. 5, lambda* = 6.296) applied to the rarest sector state of the frozen circuits -> the k = 4 r = 1 circuits get N4 = 8200 (B=0) / 19700 (B=1) on the 2026-09-21 calibration at 0.82 x 0.7 f_cal (0.7 = the lower edge of criterion 2), all other counts unchanged; P(saturate) >= 0.998 per sector for any device passing criterion 2; session 69.9 s (was 46.7; cap 120), 6 jobs (was 4). The Aer prediction at that plan is 37-52 min, so gate_H0P gets a per-class sampling cache (invocations < 30 min). Part A' = engineering + FakeFez rehearsal (expected N4 11700 / 28100), then STOP 0 (owner acknowledges D3' and the budget), then part B' = prompts/15 part B with the plan file (B'2-B'4), STOP A, canary, STOP B, main, gate H0 unchanged. |
| 2026-09-21 | prompts/16_H0P_ibm_fez_fix_20260921.md (**part A' only**; STOP 0 not yet signed, part B' not started, nothing submitted, no QPU time spent) | H0P_rehearsal (new, PASS), H0P_repro + H0_dryrun (regressions) | executor-opus | **every part-A' pass criterion met.** `data/H0_shot_plan_FakeFez.json`: N4 **11700** (B=0) / **28100** (B=1), `min_lambda_r1_at_margin` **6.3238 / 6.3099** >= lambda* 6.2958, `P_saturation_frozen_plan_clean` **0.0006 / 0.0349** (memo 0.0006 / 0.035), `amplitude_crosscheck_max_dp` **1.77e-14**, QPU execution **81.0 s**. `data/H0_shot_plan_live_20260921_reference.json`: N4 **8200 / 19700**, `r1_shots_total` **45005 / 41002**, `P_saturation_frozen_plan_clean` **0.0043 / 0.0762**, with garbage **0.6036 / 0.4220**, and all **84** per-circuit f identical to `validation/H0P_ibm_fez.json`'s `f_live` (max abs diff **0.0**). **`validation/H0P_rehearsal.json` PASS 18/18** (175 s), supports **38/38 and 20/20**, `missing_states == []` both sectors, `abs_error` 0.0, r = 1 yields 0.1489 / 0.1371. Submission path: **6 jobs of 21/5/2/28/28/42 pubs**, all DONE, 126 counts files; `h0_qpu_time --shots-plan` prints the same six groups and 81.0 s. Regressions: **H0_dryrun PASS 9/9** with `criteria[*].value` identical to the committed file (only `runtime_s` and the environment block differ), the A1 one-liner prints **"H0P_repro identical"**, **57 pytest tests** pass (13 new), PACKAGE OK. | 8941d7c, (this commit) | **The fix.** New `scripts/h0_support_plan.py` implements **rule D3'**: r = 2 / 3 keep 130 / 92, every r = 1 circuit keeps the floor 267, and the k = 4 circuits of each sector get N4 = the smallest multiple of 100 such that every sector state s has expected clean count `sum_c N_c * 0.82 * 0.7 * f_c * p_c(s) >= lambda* = poisson_lambda_star(3, 0.95) = 6.295794` from the **r = 1 circuits alone**; 0.7 is the lower edge of prereg criterion 2, so a device that passes criterion 2 saturates both sectors with the manual's three-hit / 95 % guarantee. `p_c(s)` comes from the **frozen QPY** (`hardware.logical_statevector` -> `CodewordEmbedding.project`, |amp|^2 on the sector indices) and is cross-checked against `krylov.apply_groups` at the manifest's k*dt applied `repetitions` times (max |dp| 1.77e-14 over all 84 circuits). The offline `--calibration` path recomputes f from the per-edge cz and per-qubit measure errors of a `calibration_<stamp>.json` with exactly the formula of `gate_S2D.analyse_on_backend` and reproduces its values **bit for bit** (0.0 difference on all 84 circuits), which is what makes the A'2 record auditable without an account. **gate_H0P.py**: `--shots-plan` (per-circuit shots; on a live backend the plan's `calibration.last_update_date` must equal the live target's or it exits telling you to re-run the plan); a per-class **sampling cache** `--sample-cache <dir>` with `--sectors/--reps/--only-ids/--sample-only/--refresh-cache`, one file per (sector, r) plus `calibration_circuits.json`, which refuses reuse on a mismatched backend, seed, calibration date, plan stamp or per-circuit shot count (SystemExit naming the field) and accumulates partial classes so a class can be split over invocations; the **two new criteria** "<sector> shot plan: every one of the N sector states has expected clean count >= lambda* in the r = 1 circuits at 0.7 x f_cal" (18 criteria in all); and the per-state outputs `support_states_decoded`, `missing_states` (basis index + (j2; n) label) and `per_state` with the observed accepted count next to the plan's predicted clean count. `data.shot_plan_file`, `data.shot_plan_stamp`, `data.support_shot_plan` and `data.sampling.shots_by_circuit` are recorded. **h0_qpu_time.py** `--shots-plan`: `estimate(..., shots_by_circuit=)` groups by (kind, shots), so the mixed-shot guard no longer fires and the plan shows as six groups; `shots_by_circuit_source` added, `shots_by_repetition` kept for the old path. **h0_submit.py** `--shots-plan`: `plan_groups` unchanged -> 6 groups; the live preflight additionally refuses unless the plan's calibration date equals the live target's AND the plan's `shots_by_circuit` equals `validation/H0P_<backend>.json`'s `data.sampling.shots_by_circuit` (like for like). **gate_H0.py** reads the plan out of `--predict-from`'s `data.support_shot_plan` and prints the observed-vs-predicted per-state table and the missing states in section 3 (informational; the criteria block is untouched). **The 30-minute rule.** A'3 was run in **five** sampling invocations, not the four the prompt sketched, because the prompt's rate estimate (0.0262 s per r = 1 circuit-shot, from the ibm_fez Aer model) predicted 31-36 min for B=0 r = 1 alone on the FakeFez plan. The first call was therefore restricted with the **new `--only-ids`** to references 06 and 43 and measured the real cost: Aer parallelises over shots when a call has few circuits and many shots, so 25002 shots took **3m27s** (0.007 s per circuit-shot, ~4x cheaper than the two-point pilot suggests). The remaining calls were 4m59s (rest of B=0 r = 1), 6m43s (B=1 r = 1), 10m59s (r = 2 and 3, both sectors) and 2m57s (the 42 calibration circuits + the analysis, incl. pytest) - every invocation far inside the rule. Longest single command of the whole step: **19m43s** (the H0P_repro reproducibility run). Total part-A' compute about 1 h 10 min. **What the rehearsal shows.** The r = 1 circuits alone now give support 38 (B=0) and 20 (B=1); state **53** `(1,0,1,1); (1,1,2,2)` - the B=1 state that was missing on the device-day prediction and had expected clean count 0.33 under the frozen plan - was observed **29** times against a predicted clean count of 9.17 (guaranteed >= 6.31 at the 0.7 margin). Saturation is now a prediction, not a coin flip. **Discipline.** No criterion constant, tolerance, convention or decoder was changed: F_TOLERANCE 0.30, RANDOM_ACCEPT_MAX 0.01, E0_TOL 1e-6, DIAG_MIN 0.9, RO_FACTOR 3.0, READOUT_FACTOR 0.82 and `src/skqd/codec.py` / `src/skqd/reference_sim.py` are untouched; the criteria block of `gate_H0.py` was not edited; `git status data/hardware/H0_prep` is clean; `validation/H0P_ibm_fez.json` is left exactly as committed at 51ac0e6 (the FAIL record of what happened). A'4's `/tmp/plan_small.json` uses **12 shots for the B=0 k = 4 circuits and 13 for the B=1 ones** rather than one value for both: the prompt's expected job shape 21/5/2/28/28/42 needs the two k = 4 sets to be two distinct shot counts, which is exactly what rule D3' produces on a real calibration (N4 differs per sector). **Nothing was submitted, no canary was run, zero QPU seconds were spent**; the only device access in the whole step was reading the committed calibration JSON. Part B' is held at **STOP 0** for the owner. |
