# laptop / desktop extras (the physics core needs only requirements.txt)
qiskit>=1.2
qiskit-aer>=0.15
pytest>=7
matplotlib>=3.8
# optional, GPU:
# qiskit-aer-gpu>=0.15        # CUDA 12 wheels; GTX 1060 Max-Q (sm_61) is supported by Aer's GPU statevector
# cudaq                        # CUDA-Q (or use the container nvcr.io/nvidia/quantum/cuda-quantum:cu13-0.15.1)
# qiskit-ibm-runtime           # fake backends / real calibrations for gate S3, hardware for H0
